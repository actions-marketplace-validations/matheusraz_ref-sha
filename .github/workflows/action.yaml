name: Test Action

on: push

jobs:
  get_sha:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      uses: matheusraz/ref-sha@main
      id: get_sha
      with:
        ref: staging/v6
    
    - name: Result
      run: echo "${{ steps.get_sha.outputs.sha }}"